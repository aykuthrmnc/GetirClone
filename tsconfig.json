{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./src",
    "paths": {
      "~/*": ["./src/*"]
      // "@api": ["./api"] <- would work the same
    }
  }
}
